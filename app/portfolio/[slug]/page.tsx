import Link from "next/link";import {notFound} from "next/navigation";import {SiteHeader} from "@/components/SiteHeader";import {SiteFooter} from "@/components/SiteFooter";import {projects} from "@/lib/content";
export default async function Project({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=projects.find(x=>x.slug===slug);if(!p)notFound();return <><SiteHeader/><main className="detail-page"><section className="detail-head"><p>{p.category}</p><h1>{p.title}</h1><p>{p.desc}</p></section><div className={`detail-cover tone-${p.tone}`}><span>{p.monogram}</span></div><section className="detail-info"><p className="section-label">PROJECT OVERVIEW</p><div><h2>흩어진 문제를 하나의<br/>명확한 경험으로 정리했습니다.</h2><p>{p.desc} 프로젝트의 목적과 고객 접점을 분석해 핵심 메시지, 시각 언어, 실제 활용 기준을 함께 구축했습니다.</p><dl><div><dt>SCOPE</dt><dd>Strategy · Design · System</dd></div><div><dt>YEAR</dt><dd>2026</dd></div><div><dt>SERVICE</dt><dd>{p.category}</dd></div></dl></div></section><Link className="next-link" href="/portfolio">← ALL PROJECTS</Link></main><SiteFooter/></>}
