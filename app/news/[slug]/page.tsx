import {notFound} from "next/navigation";import {SiteHeader} from "@/components/SiteHeader";import {SiteFooter} from "@/components/SiteFooter";import {newsItems} from "@/lib/content";
export default async function News({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const n=newsItems.find(x=>x.slug===slug);if(!n)notFound();return <><SiteHeader/><main className="article-page"><header><p>{n.date} · {n.category}</p><h1>{n.title}</h1></header><div className={`article-cover tone-${n.tone}`}>{n.tag}</div><article><p>언디파이너리 솔루션은 기업 안에 흩어진 브랜드와 운영 환경을 진단하고, 실제로 사용할 수 있는 하나의 기준으로 연결합니다.</p><h2>결과물보다 먼저 구조를 생각합니다.</h2><p>프로젝트의 목적과 현재 상황을 파악한 뒤 전략, 디자인, 개발, 제작과 운영이 같은 방향으로 이어지도록 설계합니다. 새로운 소식과 프로젝트 진행 과정은 이 공간에 계속 업데이트됩니다.</p></article><a className="next-link" href="/about#news">← BACK TO NEWS</a></main><SiteFooter/></>}
