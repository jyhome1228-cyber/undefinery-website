const grid=document.querySelector('#glow-grid');if(grid){const labels=['BRAND SYSTEM','PACKAGE','WEB & SITE','OPERATION','AI MODEL','AI SPACE','BUSINESS SYSTEM','EDITORIAL','PRODUCTION','CONTENTS','STRATEGY','EVENT'];const count=innerWidth<800?45:72;for(let i=0;i<count;i++){const card=document.createElement('div');card.className='glow-card';card.dataset.label=labels[i%labels.length];grid.appendChild(card)}const cards=[...document.querySelectorAll('.glow-card')];let pointer={x:innerWidth/2,y:innerHeight/2},frame;const paint=()=>{document.documentElement.style.setProperty('--mx',`${pointer.x}px`);document.documentElement.style.setProperty('--my',`${pointer.y}px`);cards.forEach(card=>{const box=card.getBoundingClientRect(),cx=box.left+box.width/2,cy=box.top+box.height/2,d=Math.hypot(pointer.x-cx,pointer.y-cy),glow=Math.max(0,1-d/300);card.style.setProperty('--glow',glow.toFixed(3));card.style.setProperty('--lx',`${pointer.x-box.left}px`);card.style.setProperty('--ly',`${pointer.y-box.top}px`);card.style.transform=`perspective(700px) translate3d(0,${glow*-6}px,${glow*12}px)`});frame=null};addEventListener('pointermove',e=>{pointer={x:e.clientX,y:e.clientY};if(!frame)frame=requestAnimationFrame(paint)});paint()}
const button=document.querySelector('.menu-button'),panel=document.querySelector('.mobile-panel');if(button&&panel){const setMenu=open=>{button.setAttribute('aria-expanded',String(open));panel.classList.toggle('open',open);panel.setAttribute('aria-hidden',String(!open));document.body.classList.toggle('menu-open',open)};button.addEventListener('click',()=>setMenu(button.getAttribute('aria-expanded')!=='true'));panel.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>setMenu(false)));addEventListener('keydown',e=>{if(e.key==='Escape')setMenu(false)})}
document.querySelectorAll('[data-filter]').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('[data-filter]').forEach(x=>x.classList.remove('active'));button.classList.add('active');document.querySelectorAll('[data-category]').forEach(card=>{card.hidden=button.dataset.filter!=='all'&&card.dataset.category!==button.dataset.filter})}));
